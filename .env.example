# OnTheRoad Environment Variable Contract
# All variables must be documented here and registered in docs/governance/governance.json.
# SEC-001: Values must never be committed to Git.

# CI Signing Keys (Optional for local debug, required for release CI builds)
# Injected via GitHub Actions Secrets into transient build environment.
ANDROID_KEYSTORE_BASE64=""
KEYSTORE_PASSWORD=""
KEY_ALIAS=""
KEY_PASSWORD=""

# Backend API Configuration (Planned)
# Configured via buildConfigField / Gradle
API_BASE_URL="https://api.ontheroad.example.com"
